{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"PixTrail Documentation","text":"<p>Welcome to the PixTrail documentation. PixTrail is a tool that extracts GPS data from photos and generates GPX files that can be used in mapping applications.</p>"},{"location":"#what-is-pixtrail","title":"What is PixTrail?","text":"<p>PixTrail extracts the GPS information stored in your photos' EXIF metadata and converts it into standard GPX format that can be used in various mapping applications. This allows you to visualize and share your journeys based on the places where you've taken photos.</p>"},{"location":"#key-features","title":"Key Features","text":"<ul> <li>Extract GPS coordinates and timestamps from EXIF metadata in photos</li> <li>Generate GPX files with waypoints and tracks</li> <li>Support for various image formats (JPG, PNG, TIFF, BMP)</li> <li>Support for various RAW formats (CR2, NEF, ARW, ORF, etc.)</li> <li>Command-line interface for easy use</li> <li>Web interface for browser-based operation</li> <li>Process directories recursively</li> <li>Drag &amp; drop interface in the web version</li> <li>Visualize routes on OpenStreetMap</li> </ul>"},{"location":"#privacy","title":"Privacy","text":"<p>PixTrail processes all photo metadata locally on your device. No data is uploaded to any server, shared with third parties, or sent anywhere outside your computer. Your location data and photos remain completely private and under your control at all times.</p>"},{"location":"#documentation-sections","title":"Documentation Sections","text":"<ul> <li>User Guide: Learn how to install and use PixTrail</li> <li>Web Interface: Documentation for the browser-based interface</li> <li>API Reference: Detailed reference for the Python API</li> </ul>"},{"location":"#quick-start","title":"Quick Start","text":""},{"location":"#installation","title":"Installation","text":"<pre><code># Basic installation\npip install pixtrail\n\n# With web interface support\npip install pixtrail[web]\n</code></pre>"},{"location":"#command-line-usage","title":"Command Line Usage","text":"<pre><code># Process photos in a directory\npixtrail -i /path/to/photos\n\n# Start the web interface\npixtrail -w\n</code></pre>"},{"location":"#python-api","title":"Python API","text":"<pre><code>from pixtrail.core import PixTrail\n\n# Create a PixTrail object\npt = PixTrail()\n\n# Process and generate in one step\npt.process_and_generate(\"/path/to/photos\", \"/path/to/output.gpx\", recursive=True)\n</code></pre>"},{"location":"api/","title":"PixTrail API Reference","text":"<p>This document provides a detailed reference for the PixTrail Python API.</p>"},{"location":"api/#core-module","title":"Core Module","text":"<p>The <code>core</code> module provides the main functionality of PixTrail.</p>"},{"location":"api/#pixtrail-class","title":"PixTrail Class","text":"<pre><code>class PixTrail:\n    \"\"\"Main class for extracting GPS data from images and generating GPX files.\"\"\"\n</code></pre>"},{"location":"api/#methods","title":"Methods","text":""},{"location":"api/#__init__","title":"<code>__init__()</code>","text":"<p>Initialize a new PixTrail object.</p> <pre><code>def __init__(self):\n    \"\"\"Initialize the PixTrail object.\"\"\"\n</code></pre>"},{"location":"api/#process_directoryinput_dir-recursivefalse","title":"<code>process_directory(input_dir, recursive=False)</code>","text":"<p>Process all image files in a directory and extract GPS data.</p> <pre><code>def process_directory(self, input_dir, recursive=False):\n    \"\"\"\n    Process all image files in a directory and extract GPS data.\n\n    Args:\n        input_dir: Directory containing image files\n        recursive: Whether to search recursively in subdirectories\n\n    Returns:\n        List of dictionaries containing GPS data extracted from images\n    \"\"\"\n</code></pre>"},{"location":"api/#generate_gpxoutput_path-gps_data_listnone","title":"<code>generate_gpx(output_path, gps_data_list=None)</code>","text":"<p>Generate a GPX file from the extracted GPS data.</p> <pre><code>def generate_gpx(self, output_path, gps_data_list=None):\n    \"\"\"\n    Generate a GPX file from the extracted GPS data.\n\n    Args:\n        output_path: Path where the GPX file will be saved\n        gps_data_list: List of dictionaries containing GPS data\n                      (if None, use the data extracted by process_directory)\n\n    Returns:\n        bool: True if the GPX file was generated successfully, False otherwise\n    \"\"\"\n</code></pre>"},{"location":"api/#process_and_generateinput_dir-output_pathnone-recursivefalse","title":"<code>process_and_generate(input_dir, output_path=None, recursive=False)</code>","text":"<p>Process all images in a directory and generate a GPX file.</p> <pre><code>def process_and_generate(self, input_dir, output_path=None, recursive=False):\n    \"\"\"\n    Process all images in a directory and generate a GPX file.\n\n    Args:\n        input_dir: Directory containing image files\n        output_path: Path where the GPX file will be saved\n        recursive: Whether to search recursively in subdirectories\n\n    Returns:\n        bool: True if the GPX file was generated successfully, False otherwise\n    \"\"\"\n</code></pre>"},{"location":"api/#process_singlepixtrail-args","title":"<code>process_single(pixtrail, args)</code>","text":"<p>Process a single directory and generate a GPX file.</p> <pre><code>def process_single(pixtrail: PixTrail, args: argparse.Namespace) -&gt; int:\n    \"\"\"\n    Process a single directory and generate a GPX file.\n\n    Args:\n        pixtrail: PixTrail object\n        args: Parsed arguments\n\n    Returns:\n        Exit code (0 for success, non-zero for failure)\n    \"\"\"\n</code></pre>"},{"location":"api/#process_batchpixtrail-args","title":"<code>process_batch(pixtrail, args)</code>","text":"<p>Process multiple directories and generate GPX files for each.</p> <pre><code>def process_batch(pixtrail: PixTrail, args: argparse.Namespace) -&gt; int:\n    \"\"\"\n    Process multiple directories and generate GPX files for each.\n\n    Args:\n        pixtrail: PixTrail object\n        args: Parsed arguments\n\n    Returns:\n        Exit code (0 for success, non-zero for failure)\n    \"\"\"\n</code></pre>"},{"location":"api/#exif-reader-module","title":"EXIF Reader Module","text":"<p>The <code>exif_reader</code> module provides functionality for extracting EXIF data from image files.</p>"},{"location":"api/#exifreader-class","title":"ExifReader Class","text":"<pre><code>class ExifReader:\n    \"\"\"Class for reading EXIF data from image files, focusing on GPS information.\"\"\"\n</code></pre>"},{"location":"api/#methods_1","title":"Methods","text":""},{"location":"api/#extract_gps_dataimage_path","title":"<code>extract_gps_data(image_path)</code>","text":"<p>Extract GPS data from an image file.</p> <pre><code>@staticmethod\ndef extract_gps_data(image_path):\n    \"\"\"\n    Extract GPS data from an image file.\n\n    Args:\n        image_path: Path to the image file\n\n    Returns:\n        Dictionary containing GPS information (latitude, longitude, altitude, timestamp)\n        or None if no GPS data is found\n    \"\"\"\n</code></pre>"},{"location":"api/#_extract_gps_with_pillowimage_path","title":"<code>_extract_gps_with_pillow(image_path)</code>","text":"<p>Extract GPS data using Pillow as a fallback method.</p> <pre><code>@staticmethod\ndef _extract_gps_with_pillow(image_path):\n    \"\"\"\n    Extract GPS data using Pillow as a fallback method.\n\n    Args:\n        image_path: Path to the image file\n\n    Returns:\n        Dictionary containing GPS information or None if no GPS data is found\n    \"\"\"\n</code></pre>"},{"location":"api/#_convert_to_degreesvalue","title":"<code>_convert_to_degrees(value)</code>","text":"<p>Convert GPS coordinates from degrees, minutes, seconds format to decimal degrees.</p> <pre><code>@staticmethod\ndef _convert_to_degrees(value):\n    \"\"\"\n    Convert GPS coordinates from degrees, minutes, seconds format to decimal degrees.\n\n    Args:\n        value: Tuple of (degrees, minutes, seconds)\n\n    Returns:\n        Decimal degrees as a float\n    \"\"\"\n</code></pre>"},{"location":"api/#gpx-generator-module","title":"GPX Generator Module","text":"<p>The <code>gpx_generator</code> module provides functionality for generating GPX files.</p>"},{"location":"api/#gpxgenerator-class","title":"GPXGenerator Class","text":"<pre><code>class GPXGenerator:\n    \"\"\"Class for generating GPX files from GPS data.\"\"\"\n</code></pre>"},{"location":"api/#methods_2","title":"Methods","text":""},{"location":"api/#create_gpxgps_data_list-output_path","title":"<code>create_gpx(gps_data_list, output_path)</code>","text":"<p>Create a GPX file from a list of GPS data points.</p> <pre><code>@staticmethod\ndef create_gpx(gps_data_list, output_path):\n    \"\"\"\n    Create a GPX file from a list of GPS data points.\n\n    Args:\n        gps_data_list: List of dictionaries containing GPS data\n                       (latitude, longitude, altitude, timestamp, name)\n        output_path: Path where the GPX file will be saved\n\n    Returns:\n        bool: True if the GPX file was created successfully, False otherwise\n    \"\"\"\n</code></pre>"},{"location":"api/#add_waypoint_to_gpxgpx_file-latitude-longitude-namenone-altitudenone-timestampnone","title":"<code>add_waypoint_to_gpx(gpx_file, latitude, longitude, name=None, altitude=None, timestamp=None)</code>","text":"<p>Add a waypoint to an existing GPX file.</p> <pre><code>@staticmethod\ndef add_waypoint_to_gpx(gpx_file, latitude, longitude, name=None, altitude=None, timestamp=None):\n    \"\"\"\n    Add a waypoint to an existing GPX file. If the file doesn't exist, create it.\n\n    Args:\n        gpx_file: Path to the GPX file\n        latitude: Waypoint latitude\n        longitude: Waypoint longitude\n        name: Waypoint name\n        altitude: Waypoint altitude\n        timestamp: Waypoint timestamp\n\n    Returns:\n        bool: True if the waypoint was added successfully, False otherwise\n    \"\"\"\n</code></pre>"},{"location":"api/#utils-module","title":"Utils Module","text":"<p>The <code>utils</code> module provides utility functions for the PixTrail package.</p>"},{"location":"api/#functions","title":"Functions","text":""},{"location":"api/#get_image_filesdirectory-recursivefalse","title":"<code>get_image_files(directory, recursive=False)</code>","text":"<p>Get a list of image files from a directory.</p> <pre><code>def get_image_files(directory, recursive=False):\n    \"\"\"\n    Get a list of image files from a directory.\n\n    Args:\n        directory: Directory to search for image files\n        recursive: Whether to search recursively in subdirectories\n\n    Returns:\n        List of paths to image files\n    \"\"\"\n</code></pre>"},{"location":"api/#ensure_directorydirectory","title":"<code>ensure_directory(directory)</code>","text":"<p>Ensure that a directory exists, create it if it doesn't.</p> <pre><code>def ensure_directory(directory):\n    \"\"\"\n    Ensure that a directory exists, create it if it doesn't.\n\n    Args:\n        directory: Directory path to ensure exists\n\n    Returns:\n        True if directory exists or was created successfully, False otherwise\n    \"\"\"\n</code></pre>"},{"location":"api/#get_default_output_pathinput_dir-filenametrackgpx","title":"<code>get_default_output_path(input_dir, filename=\"track.gpx\")</code>","text":"<p>Generate a default output path for the GPX file based on the input directory.</p> <pre><code>def get_default_output_path(input_dir, filename=\"track.gpx\"):\n    \"\"\"\n    Generate a default output path for the GPX file based on the input directory.\n\n    Args:\n        input_dir: Input directory path\n        filename: Name of the output file\n\n    Returns:\n        Default output path for the GPX file\n    \"\"\"\n</code></pre>"},{"location":"api/#validate_coordinateslatitude-longitude","title":"<code>validate_coordinates(latitude, longitude)</code>","text":"<p>Validate GPS coordinates.</p> <pre><code>def validate_coordinates(latitude, longitude):\n    \"\"\"\n    Validate GPS coordinates.\n\n    Args:\n        latitude: Latitude value to validate (-90 to 90)\n        longitude: Longitude value to validate (-180 to 180)\n\n    Returns:\n        Tuple of (is_valid, error_message)\n    \"\"\"\n</code></pre>"},{"location":"api/#cli-module","title":"CLI Module","text":"<p>The <code>cli</code> module provides the command-line interface for PixTrail.</p>"},{"location":"api/#functions_1","title":"Functions","text":""},{"location":"api/#parse_argsargsnone","title":"<code>parse_args(args=None)</code>","text":"<p>Parse command-line arguments.</p> <pre><code>def parse_args(args=None):\n    \"\"\"\n    Parse command-line arguments.\n\n    Args:\n        args: Command-line arguments (if None, use sys.argv)\n\n    Returns:\n        Parsed arguments\n    \"\"\"\n</code></pre>"},{"location":"api/#mainargsnone","title":"<code>main(args=None)</code>","text":"<p>Main entry point for the command-line interface.</p> <pre><code>def main(args=None):\n    \"\"\"\n    Main entry point for the command-line interface.\n\n    Args:\n        args: Command-line arguments (if None, use sys.argv)\n\n    Returns:\n        Exit code (0 for success, non-zero for failure)\n    \"\"\"\n</code></pre>"},{"location":"usage/","title":"PixTrail Usage Guide","text":"<p>This guide explains how to use PixTrail, a tool for extracting GPS data from photos and generating GPX files.</p>"},{"location":"usage/#installation","title":"Installation","text":""},{"location":"usage/#basic-installation","title":"Basic Installation","text":"<p>You can install PixTrail using pip:</p> <pre><code>pip install pixtrail\n</code></pre>"},{"location":"usage/#installation-with-web-interface","title":"Installation with Web Interface","text":"<p>If you want to use the web interface, install with the web extras:</p> <pre><code>pip install pixtrail[web]\n</code></pre>"},{"location":"usage/#install-from-source","title":"Install from Source","text":"<p>You can also install it from source:</p> <pre><code>git clone https://github.com/sukitsubaki/pixtrail.git\ncd pixtrail\npip install -e .\n</code></pre> <p>Or you can install it from your local package: <pre><code>python /path/to/pixtrail/setup.py install\n</code></pre></p>"},{"location":"usage/#command-line-interface","title":"Command Line Interface","text":"<p>PixTrail provides a simple command-line interface for processing photos and generating GPX files.</p>"},{"location":"usage/#basic-usage","title":"Basic Usage","text":"<pre><code>pixtrail -i /path/to/photos\n</code></pre> <p>This will process all photos in the specified directory and create a GPX file in the same directory.</p>"},{"location":"usage/#options","title":"Options","text":"<p>The following options are available:</p> <ul> <li><code>-i, --input-dir</code>: Directory containing photos with GPS data</li> <li><code>-o, --output</code>: Specify the output GPX file path</li> <li><code>-r, --recursive</code>: Search for images recursively in subdirectories</li> <li><code>-v, --verbose</code>: Enable verbose output</li> <li><code>-w, --web</code>: Start the web interface</li> <li><code>--host</code>: Host for the web interface (default: 127.0.0.1)</li> <li><code>--port</code>: Port for the web interface (default: 5000)</li> <li><code>--no-browser</code>: Don't automatically open a browser when starting the web interface</li> </ul>"},{"location":"usage/#automatic-gpx-naming","title":"Automatic GPX Naming","text":"<p>If you don't specify an output file with <code>-o</code>, PixTrail will automatically name the GPX file after the directory name containing the photos:</p> <pre><code>pixtrail -i /path/to/Photos-Kyoto\n</code></pre> <p>This will create a GPX file named <code>Photos_Kyoto.gpx</code> in the same directory.</p>"},{"location":"usage/#batch-processing","title":"Batch Processing","text":"<p>PixTrail supports processing multiple directories at once with separate GPX outputs:</p> <pre><code># Process multiple directories\npixtrail -b /path/to/photos1 /path/to/photos2 /path/to/photos3\n\n# Process multiple directories and save GPX files to a specific directory\npixtrail -b /path/to/photos1 /path/to/photos2 -d /path/to/output_dir\n\n# Process multiple directories recursively\npixtrail -b /path/to/photos1 /path/to/photos2 -r\n\n# Process multiple directories with verbose output\npixtrail -b /path/to/photos1 /path/to/photos2 -v\n</code></pre> <p>When using batch mode: - Each directory will be processed separately - A GPX file will be created for each directory, automatically named after the directory - You can optionally specify an output directory for all GPX files with <code>-d</code> - The recursive option <code>-r</code> applies to all directories in the batch</p>"},{"location":"usage/#web-interface","title":"Web Interface","text":"<p>PixTrail includes a browser-based web interface that allows you to upload photos, visualize routes, and generate GPX files, all while keeping your data on your local device:</p> <pre><code># Start the web interface with default settings\npixtrail -w\n\n# Start the web interface on a specific host and port\npixtrail -w --host 0.0.0.0 --port 8080\n\n# Start the web interface without automatically opening a browser\npixtrail -w --no-browser\n</code></pre> <p>For more details on the web interface, see the Web Interface Documentation.</p>"},{"location":"usage/#examples","title":"Examples","text":"<p>Process photos in a directory and save the GPX file to a custom location:</p> <pre><code>pixtrail -i /path/to/photos -o /path/to/output.gpx\n</code></pre> <p>Process photos recursively in a directory and its subdirectories:</p> <pre><code>pixtrail -i /path/to/photos -r\n</code></pre> <p>Enable verbose output:</p> <pre><code>pixtrail -i /path/to/photos -v\n</code></pre> <p>Start the web interface:</p> <pre><code>pixtrail -w\n</code></pre>"},{"location":"usage/#using-the-python-api","title":"Using the Python API","text":"<p>You can also use PixTrail programmatically in your Python code.</p>"},{"location":"usage/#basic-usage_1","title":"Basic Usage","text":"<pre><code>from pixtrail.core import PixTrail\n\n# Create a PixTrail object\npt = PixTrail()\n\n# Process a directory of images\nresult = pt.process_directory(\"/path/to/photos\")\ngps_data = result[\"gps_data\"]\n\n# Generate a GPX file\npt.generate_gpx(\"/path/to/output.gpx\")\n</code></pre>"},{"location":"usage/#process-and-generate-in-one-step","title":"Process and Generate in One Step","text":"<pre><code>from pixtrail.core import PixTrail\n\n# Create a PixTrail object\npt = PixTrail()\n\n# Process and generate in one step\npt.process_and_generate(\"/path/to/photos\", \"/path/to/output.gpx\", recursive=True)\n</code></pre>"},{"location":"usage/#working-with-gps-data-directly","title":"Working with GPS Data Directly","text":"<p>You can also work with the GPS data directly:</p> <pre><code>from pixtrail.core import PixTrail\nfrom pixtrail.gpx_generator import GPXGenerator\n\n# Create a PixTrail object\npt = PixTrail()\n\n# Process a directory of images\nresult = pt.process_directory(\"/path/to/photos\")\ngps_data = result[\"gps_data\"]\n\n# Manipulate the GPS data as needed\nfiltered_data = [point for point in gps_data if point[\"altitude\"] &gt; 100]\n\n# Generate a GPX file with the filtered data\nGPXGenerator.create_gpx(filtered_data, \"/path/to/output.gpx\")\n</code></pre>"},{"location":"usage/#starting-the-web-interface-programmatically","title":"Starting the Web Interface Programmatically","text":"<p>You can also start the web interface from Python code:</p> <pre><code>from pixtrail.web import start_server\n\n# Start the web interface\napp, server = start_server(host=\"127.0.0.1\", port=5000, open_browser=True)\n\n# The server runs in a background thread, so your code can continue\n# Keep your program running to keep the server running\nimport time\ntry:\n    while True:\n        time.sleep(1)\nexcept KeyboardInterrupt:\n    # Shutdown the server when Ctrl+C is pressed\n    server.shutdown()\n</code></pre>"},{"location":"usage/#handling-errors","title":"Handling Errors","text":"<p>PixTrail is designed to be robust against missing or invalid EXIF data. If an image does not contain GPS data, it will be skipped. If there are errors reading EXIF data with the primary method (exifread), PixTrail will fall back to using Pillow.</p> <p>If no images with GPS data are found, PixTrail will print an error message and exit with a non-zero status code.</p>"},{"location":"usage/#using-the-gpx-file","title":"Using the GPX File","text":"<p>The generated GPX file can be imported into various mapping applications:</p> <ul> <li>OpenStreetMap</li> <li>Google Earth</li> <li>GPS viewers</li> <li>Mapping applications on smartphones and GPS devices</li> </ul> <p>The GPX file contains both waypoints (each representing a photo) and a track (connecting the waypoints in chronological order).</p>"},{"location":"web_interface/","title":"Web Interface Documentation","text":"<p>This document provides details about the PixTrail web interface, including how to start it, its features, and how it processes photos.</p>"},{"location":"web_interface/#starting-the-web-interface","title":"Starting the Web Interface","text":"<p>You can start the web interface using the command-line interface with the <code>-w</code> or <code>--web</code> option:</p> <pre><code># Start with default settings (localhost:5000)\npixtrail -w\n\n# Specify host and port\npixtrail -w --host 0.0.0.0 --port 8080\n\n# Start without automatically opening a browser\npixtrail -w --no-browser\n</code></pre> <p>The web interface starts a local web server on your machine and automatically opens a browser window pointing to the interface.</p>"},{"location":"web_interface/#processing-photos","title":"Processing Photos","text":"<p>The PixTrail web interface offers two main methods for processing photos:</p> <ol> <li>Individual Photos: Upload and process individual image files</li> <li>Directory: Process an entire directory of photos</li> </ol>"},{"location":"web_interface/#hybrid-processing-approach","title":"Hybrid Processing Approach","text":"<p>PixTrail uses a hybrid approach to process different types of image files:</p> <ul> <li>Client-side Processing (In-Browser):</li> <li>JPEG/TIFF files are processed directly in your browser</li> <li>EXIF data is extracted using the browser's JavaScript capabilities</li> <li>Only the extracted GPS coordinates are sent to the server</li> <li> <p>This provides faster processing and reduced bandwidth usage</p> </li> <li> <p>Local Server-side Processing:</p> </li> <li>RAW files (CR2, NEF, ARW, etc.) and other formats are processed on the server</li> <li>These files are temporarily uploaded to the server for processing</li> <li>After extracting GPS data, the original files are automatically deleted</li> <li>Only the generated GPX file is stored</li> </ul>"},{"location":"web_interface/#file-selection","title":"File Selection","text":"<p>You can select files in several ways:</p> <ul> <li>Click the \"Select Photos\" button to choose individual files</li> <li>Click the \"Select Directory\" button to choose an entire folder</li> <li>Drag and drop image files directly into the interface</li> </ul>"},{"location":"web_interface/#recursive-directory-processing","title":"Recursive Directory Processing","text":"<p>When processing a directory, you can choose to process subdirectories recursively:</p> <ol> <li>Check the \"Process subdirectories recursively\" option</li> <li>Optionally specify a depth level (0 = all levels)</li> </ol>"},{"location":"web_interface/#viewing-results","title":"Viewing Results","text":"<p>After processing, PixTrail will:</p> <ol> <li>Display a map showing the route derived from your photos</li> <li>Add markers for each photo location</li> <li>Show a timeline of the journey based on photo timestamps</li> <li>Provide statistics about the processed photos</li> </ol>"},{"location":"web_interface/#advanced-visualization-features","title":"Advanced Visualization Features","text":""},{"location":"web_interface/#heat-map","title":"Heat Map","text":"<p>The heat map feature visualizes where you spent the most time during your journey:</p> <ol> <li>Click the \"Show Heatmap\" button in the map controls to toggle the heat map</li> <li>Areas with higher intensity (red/yellow) indicate:</li> <li>More photos taken at that location</li> <li>Longer time spent at that location (based on photo timestamps)</li> <li>Areas with lower intensity (blue/green) indicate brief or single-photo stops</li> <li>The heat map can be enabled simultaneously with the clustering feature</li> </ol>"},{"location":"web_interface/#marker-clustering","title":"Marker Clustering","text":"<p>For routes with many photos, clustering helps keep the map clean and navigable:</p> <ol> <li>Click the \"Enable Clustering\" button to group nearby photos into clusters</li> <li>Each cluster shows the number of photos it contains</li> <li>Click a cluster to zoom in and see individual photos</li> <li>Use the radius slider to adjust how aggressively photos are grouped</li> <li>Smaller radius: More individual markers, fewer clusters</li> <li>Larger radius: Fewer, larger clusters</li> <li>The clustering feature works well for dense city tours or locations with many photos</li> </ol>"},{"location":"web_interface/#route-statistics","title":"Route Statistics","text":"<p>The statistics panel provides detailed metrics about your journey:</p> <ol> <li>Click the \"Show Statistics\" button to open the statistics panel</li> <li>View summary statistics:</li> <li>Total distance traveled</li> <li>Journey duration</li> <li>Average and maximum speeds</li> <li>Elevation data (min/max elevation and total elevation gain)</li> <li>Start and end times</li> <li>Total number of photos</li> <li>Interactive charts visualize:</li> <li>Elevation profile throughout your journey</li> <li>Speed variations between waypoints</li> <li>These calculations are based on the GPS coordinates and timestamps from your photos</li> </ol>"},{"location":"web_interface/#downloading-gpx-files","title":"Downloading GPX Files","text":"<p>You can download the generated GPX file by clicking the \"Download GPX\" button. This file can be imported into:</p> <ul> <li>Google Earth</li> <li>Garmin GPS devices</li> <li>OpenStreetMap</li> <li>Any GPX-compatible mapping application</li> </ul>"},{"location":"web_interface/#privacy-and-data-handling","title":"Privacy and Data Handling","text":"<p>The PixTrail web interface runs entirely on your local machine. No photo data is sent to external servers:</p> <ul> <li>For JPEG/TIFF files, only the extracted GPS coordinates are sent to the local server</li> <li>For RAW/PNG files, files are temporarily cached locally during processing</li> <li>All temporary files are automatically deleted after processing</li> <li>Only the generated GPX files are stored in the <code>__pixtrail-cache__</code> directory</li> </ul>"},{"location":"web_interface/#technical-details","title":"Technical Details","text":"<ul> <li>The web interface is built with Flask and modern web technologies</li> <li>The client-side uses JavaScript and the EXIF.js library for metadata extraction</li> <li>Chart visualizations are powered by Chart.js</li> <li>The server runs only on your local machine (not on the internet)</li> <li>The interface is responsive and works on various screen sizes</li> </ul>"}]}